package services

import (
	"testing"
)

func TestPredictTaoCan(t *testing.T) {
	/*
		c := config.ConfigData
		bot := NewSession(c)
		defer bot.Close()
		input := &TaoCanInput{
			Age:  20,
			Flow: 8345,
			Call: 90,
			Cost: 30,
		}
		taocan := bot.PredictTaoCan(input)
		t.Logf("The predicted taocan is %s", taocan)
	*/
}
